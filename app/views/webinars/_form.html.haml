= simple_form_for @webinar, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.error_span :name
  = f.input :theme
  = f.error_span :theme
  = f.input :rate
  = f.button :submit, :class => 'btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), webinars_path, :class => 'btn btn-default'
